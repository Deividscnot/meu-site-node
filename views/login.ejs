<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login - BGtecnologia</title>
  <link rel="icon" href="/static/favicon.ico" type="image/x-icon">
  <style>
    :root {
      --primary: #00ffc8;
      --primary-dark: #00ffc8;
      --bg: #f5f5f5;
      --text: #333;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0; font-family: system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
      background: var(--bg); color: var(--text);
    }
    /* Header */
    header {
      background: var(--primary);
      color: #fff;
      display: flex; align-items: center; justify-content: space-between;
      padding: 14px 22px;
    }
    header .brand { font-weight: 700; letter-spacing: .3px; }
    nav a {
      color: #fff; text-decoration: none; margin-left: 16px; font-weight: 600;
    }
    nav a:hover { text-decoration: underline; }

    /* Card de login */
    .wrap {
      min-height: calc(100vh - 60px);
      display: grid; place-items: center; padding: 24px;
    }
    .card {
      width: 100%; max-width: 420px;
      background: #fff; border-radius: 12px; padding: 28px;
      box-shadow: 0 10px 28px rgba(0,0,0,.12);
    }
    h1 { margin: 0 0 12px; font-size: 1.4rem; color: var(--primary-dark); }
    label { display: block; margin: 12px 0 6px; font-size: .95rem; }
    input {
      width: 100%; padding: 12px; border: 1px solid #ccc; border-radius: 8px; font-size: 1rem;
    }
    input:focus { outline: none; border-color: var(--primary); }
    button {
      width: 100%; padding: 12px; margin-top: 16px;
      background: var(--primary); color: #fff; border: 0; border-radius: 8px;
      font-weight: 700; cursor: pointer;
    }
    button:hover { background: var(--primary-dark); }

    .error {
      background: #ffe4e6; color: #00ffc8;
      border: 1px solid #fecdd3;
      padding: 10px; border-radius: 8px; margin-top: 12px; text-align: center;
      font-size: .95rem;
    }
    .links {
      display: flex; justify-content: space-between; gap: 10px; margin-top: 14px;
      font-size: .95rem;
    }
    .links a { color: var(--primary-dark); text-decoration: none; }
    .links a:hover { text-decoration: underline; }

    footer {
      text-align: center; color: #777; font-size: .85rem; padding: 18px 12px 28px;
    }
  </style>
</head>
<body>
  <header>
    <div class="brand">BGtecnologia</div>
    <nav>
      <a href="/sobre">Sobre nós</a>
      <a href="/produtos">Produtos</a>
      <a href="/termo">Termos</a>
      <!-- Não mostramos “Área do cliente” quando não logado -->
      <!-- Se quiser deixar “Recarga online” público, descomente a linha abaixo -->
      <!-- <a href="/comprar-tokens">Recarga online</a> -->
    </nav>
  </header>

  <div class="wrap">
    <div class="card">
      <h1>Entrar</h1>

      <% if (error) { %>
        <div class="error"><%= error %></div>
      <% } %>

      <form action="/login" method="POST" novalidate>
        <label for="username">Usuário ou e-mail</label>
        <input id="username" name="username" required autocomplete="username"/>

        <label for="password">Senha</label>
        <input id="password" name="password" type="password" required autocomplete="current-password"/>

        <button type="submit">Entrar</button>
      </form>

      <div class="links">
        <a href="/register">Não tem conta? Cadastre-se</a>
        <a href="/termo">Ler Termos de Uso</a>
      </div>
    </div>
  </div>

  <footer>
    © <%= new Date().getFullYear() %> BGtecnologia — Uso profissional e legal conforme Termos de Uso.
  </footer>
</body>
</html>
