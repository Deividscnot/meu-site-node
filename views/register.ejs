<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Criar conta - BGtecnologia</title>
  <link rel="icon" href="/static/favicon.ico" type="image/x-icon">
  <link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
  <style>
    :root{--brand:#00ffc8;--brand2:#00ffc8;--text:#1f2937;--muted:#6b7280;--line:#e5e7eb;--bg:#fff;--radius:14px;--shadow:0 12px 28px rgba(0,0,0,.08)}
    *{box-sizing:border-box;margin:0;padding:0}
    body{background:#fff;color:var(--text);font:16px/1.45 "Inter",system-ui,-apple-system,Segoe UI,Roboto,Arial}
    .navbar{position:sticky;top:0;background:#fff;border-bottom:1px solid var(--line)}
    .navbar .inner{max-width:1050px;margin:0 auto;display:flex;align-items:center;gap:20px;padding:14px 20px}
    .logo{display:flex;align-items:center;gap:10px;font-weight:800}
    .logo img{width:32px;height:32px}
    .container{max-width:860px;margin:0 auto;padding:24px 20px}
    .card{background:#fff;border:1px solid var(--line);border-radius:var(--radius);box-shadow:var(--shadow)}
    .card .body{padding:22px}
    h1{font-size:26px;margin:0 0 6px}
    .sub{color:var(--muted);margin-bottom:14px}
    .grid{display:grid;gap:12px}
    @media(min-width:860px){.grid-2{grid-template-columns:1fr 1fr}}
    label span{display:block;font-size:13px;color:#6b7280;margin-bottom:6px}
    .input,select{width:100%;padding:12px 14px;border:1.5px solid var(--line);border-radius:10px;background:#fff;font-size:16px;outline:none}
    .input:focus,select:focus{border-color:var(--brand);box-shadow:0 0 0 3px rgba(255,61,0,.12)}
    .row{display:flex;gap:10px;align-items:center;justify-content:space-between;margin-top:12px}
    .btn{border:0;border-radius:10px;background:var(--brand);color:#fff;font-weight:700;padding:12px 18px;cursor:pointer}
    .btn:hover{background:var(--brand2)}
    .btn-light{background:#fff;border:1px solid var(--line);color:#111}
    .alert{padding:12px 14px;border-radius:10px;border:1px solid #fde7d9;background:#fff6ef;color:#7a2e12;margin-bottom:12px}
    .terms{border:1px solid var(--line);border-radius:10px;padding:12px;background:#fff;max-height:140px;overflow:auto}
    .muted{color:#9aa0a6;font-size:12px}
    .chk{width:18px;height:18px;accent-color:var(--brand)}
    .pw-hint{font-size:12px;color:#6b7280}
    .error{background:#ffe4e6;border:1px solid #fecdd3;color:#9f1239;padding:10px;border-radius:8px;margin-bottom:12px}
    .ok{background:#ecfdf5;border:1px solid #d1fae5;color:#065f46;padding:10px;border-radius:8px;margin-bottom:12px}
  </style>
</head>
<body>
  <header class="navbar">
    <div class="inner">
      <div class="logo"><img src="/static/img/logo_empresa.png" alt=""><span>BGtecnologia</span></div>
      <div style="margin-left:auto;display:flex;gap:10px">
        <a class="btn-light" href="/login">Já tenho conta</a>
      </div>
    </div>
  </header>

  <div class="container">
    <section class="card">
      <div class="body">
        <h1>Criar conta</h1>
        <div class="sub">Cadastro profissional — seus dados não são compartilhados com terceiros.</div>

        <% if (error) { %><div class="error"><%= error %></div><% } %>

        <form action="/register" method="POST" id="regForm" class="grid grid-2" novalidate>
          <label><span>Nome completo *</span><input class="input" name="fullName" required></label>
          <label><span>E-mail (login) *</span><input class="input" name="email" type="email" required></label>

          <label><span>Senha *</span><input class="input" name="password" id="pw" type="password" minlength="8" required></label>
          <label><span>Confirmar senha *</span><input class="input" name="password2" id="pw2" type="password" minlength="8" required></label>
          <div class="pw-hint" style="grid-column:1/-1">Use ao menos 8 caracteres. Recomendado: letras maiúsculas/minúsculas, números e símbolo.</div>

          <label><span>WhatsApp *</span><input class="input" name="whatsapp" placeholder="(xx) 9xxxx-xxxx" required></label>
          <label><span>Empresa / Oficina</span><input class="input" name="company" placeholder="Opcional"></label>

          <label><span>Cidade *</span><input class="input" name="city" required></label>
          <label><span>UF *</span>
            <select name="state" class="input" required>
              <option value="">Selecione</option>
              <% ["AC","AL","AP","AM","BA","CE","DF","ES","GO","MA","MT","MS","MG","PA","PB","PR","PE","PI","RJ","RN","RS","RO","RR","SC","SP","SE","TO"].forEach(UF => { %>
                <option value="<%= UF %>"><%= UF %></option>
              <% }) %>
            </select>
          </label>

          <label><span>CPF/CNPJ</span><input class="input" name="doc" placeholder="Opcional"></label>
          <label><span>Como nos conheceu?</span>
            <select name="source" class="input">
              <option value="">Selecione</option>
              <option>Indicação</option>
              <option>YouTube</option>
              <option>Instagram</option>
              <option>Google</option>
              <option>Outro</option>
            </select>
          </label>

          <div style="grid-column:1/-1">
            <div class="terms" id="termsBox">
              <p><b>Termos de uso (resumo)</b></p>
              <ul>
                <li>Uso exclusivo profissional, vedado qualquer uso ilícito ou fraudulento;</li>
                <li>Responsabilidade integral do operador pelos arquivos gerados;</li>
                <li>Atuar somente com autorização do proprietário do veículo;</li>
                <li>Dados tratados conforme nossa política de privacidade;</li>
              </ul>
              <p class="muted">Role até o fim para habilitar a aceitação. <a href="/termo">Ver termo completo</a>.</p>
            </div>
            <label style="display:flex;gap:8px;align-items:center;margin-top:8px">
              <input type="checkbox" class="chk" id="accept" name="accept" disabled>
              <span>Li e concordo com os termos</span>
            </label>
          </div>

          <div class="row" style="grid-column:1/-1">
            <button class="btn" type="submit" id="submitBtn" disabled>Criar conta</button>
            <a class="btn-light" href="/login">Cancelar</a>
          </div>
        </form>
      </div>
    </section>
  </div>

  <script>
    const box = document.getElementById('termsBox');
    const accept = document.getElementById('accept');
    const submitBtn = document.getElementById('submitBtn');
    const pw = document.getElementById('pw'), pw2 = document.getElementById('pw2');
    function sc(){ if(box.scrollTop + box.clientHeight >= box.scrollHeight - 3){ accept.disabled = false; } }
    box.addEventListener('scroll', sc); box.addEventListener('keyup', sc);
    accept.addEventListener('change', ()=> submitBtn.disabled = !accept.checked);
    document.getElementById('regForm').addEventListener('submit', (e)=>{
      if(pw.value !== pw2.value){ e.preventDefault(); alert('As senhas não conferem.'); pw2.focus(); }
      if(!accept.checked){ e.preventDefault(); alert('Você precisa aceitar os termos.'); }
    });
  </script>
</body>
</html>
